import styled from '@emotion/styled'

export const Input = styled.input`
    width: 60%;
    border-radius: 2rem;
    height: 40px;
    align-items: center;
    background-color: var(--gris2);
    color: white;
`;